"""designice - a vector-matched design library that builds standalone Next.js sites.

Public surface is deliberately small; the CLI (`designice`) and the MCP server
(`designice.mcp_server`) are the two intended entry points.
"""

__version__ = "0.1.0"
