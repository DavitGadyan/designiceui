/**
 * The same tokens as globals.css, in TypeScript, for the places CSS cannot
 * reach - canvas, WebGL materials, chart libraries, meta tags.
 *
 * Generated by `designice scaffold` from "{{REFERENCE_TITLE}}". Keep the two in
 * sync; if they drift, the 3D scene stops matching the page around it.
 */
export const tokens = {
  color: {
    ground: "{{COLOR_GROUND}}",
    surface: "{{COLOR_SURFACE}}",
    ink: "{{COLOR_INK}}",
    muted: "{{COLOR_MUTED}}",
    accent: "{{COLOR_ACCENT}}",
    accent2: "{{COLOR_ACCENT2}}",
  },
  palette: {{PALETTE_JSON}},
  motion: {
    ease: [0.22, 1, 0.36, 1] as const,
    fast: 180,
    base: 320,
    slow: 600,
  },
  mode: "{{COLOR_SCHEME}}" as const,
} as const;

export type Tokens = typeof tokens;
