# Critique - buyme vs nowutalk-ai-sales-agent-for-shopify

**Verdict:** Fix 1 must-fix item, then ship (8 should-fix items are worth a second pass - the structure is right, the surface is thin)
**Reviewed:** desktop + mobile pixels (`verify/critique/`, 1440x900 and 390x844). `next build` green (run on a scratch copy so the live dev server was untouched). No console errors, no horizontal overflow at either width.

Reference scale note: the Home tile in `image_original2.png` is 548px wide for a 1680px page (x0.326); reference px below are at 1680 - multiply by 0.86 to compare with the 1440px capture.

## Region fidelity
| # | Region (reference) | Build | Status | Note |
|---|---|---|---|---|
| 1 | nav - wordmark left, 5 links centred, gradient pill right, 1px hairline, active link bold | header | match | hairline only appears after 24px scroll |
| 2 | hero - text ~47% left, phone mockup + chat bubble + orb right, waveform behind, 2 pills, Shopify badge | hero | drift | composition matches; phone is a wireframe with grey tiles and 36% empty screen |
| 3 | agent-features - centred H2 (~57px) + subline, 3 gradient cards (product-card illustration + orb, title, body), accent line + black pill | agent-features | drift | H2 36px; illustrations are text bubbles only; accent line 13px |
| 4 | shopify - left H2, 50/50: white card with full storefront mockup + badge + pill / black card (illustration left, text right) + gradient card (text left, 4 stacked payment tiles right); paragraph | shopify | drift | right cards text-only; wireframe phone; extra button; grey band |
| 5 | install-steps - 2-line H2, "How it Works" tab, gradient band with 3 icon/title/body columns and chevrons, accent line + pill | install | drift | numbered sub-cards, no icons/chevrons/tab; band height matches (219 vs ~222px) |
| 6 | demo - centred H2 + 2-line subline, 16:9 frame ~73% of container with photo, "Watch Demo" black pill, caption strip | demo | drift | no poster photo; bare white circle; grey band |
| 7 | intelligence - left H2 + subline, 3x2 icon/title/body, accent line + pill | intelligence | match | H2/eyebrow scale only |
| 8 | faq - 1/3:2/3, 7 discrete surface-filled rows on white, open row white with gradient border, chevrons | faq | drift | one white card with dividers on a grey band, 8 rows, +/x glyphs |
| 9 | cta - black rounded banner ~2.95:1, H2 + body + dark ghost pill, app-icon tile right, waveform inside | cta | drift | 3.48:1; white pill; dead link |
| 10 | footer - 2fr 1fr 1fr, wordmark + blurb + 4 icon glyphs, Product 5 / Legal 2, hairline, copyright | footer | match | text social labels, no hairline |

## Must fix
1. **The install funnel ends on `href="#"`** - Reference: `image_original1.png` nav "Invest" and the hero pills are page links; `home` tile CTA banner "Install on Shopify" is the closing action. Build: `desktop-cta.png` "Install on Shopify" is `href="#"` (`components/sections/Cta.tsx:23`); the nav CTA, hero "Try it for free" and all four section pills route to `#cta`, so every path ends on a link that jumps to the top. 13 `href="#"` anchors in total (CTA, wordmark, Home, 3 socials, 7 footer links). Fix: `Cta.tsx:23` -> the app-store / `/install` URL; `Footer.tsx:21,29,35` -> real routes (`/features`, `/pricing`, `/updates`, `/privacy`, `/terms`, social URLs). `Nav.tsx:60,87` can keep `#cta` once the banner link resolves.

## Should fix
2. **Section heads and accent lines are under-scaled** - Reference: every section H2 ("Meet Nowutalk...", "Built for Shopify.", "One-Click Install.", "Frequently Asked Questions", "Let Nowutalk Do the Selling") measures ~56-60px at 1680 (~49px at 1440) - the same size as the hero H1; the accent lines under each card group ("Let Nowutalk do the selling!", "That's it...", "Turn your store...") are ~21px gradient text. Build: manifest - all seven `h2` = 36px against `h1` 52px (ratio 0.69 vs ~1.0); `.eyebrow` = 13px solid accent. Fix: `app/globals.css` `--size-h2: clamp(2rem, 3.5vw, 3.25rem)`; `.eyebrow { font-size: 18px; font-weight: 600 }` and add `gradient-text` to the three eyebrow `<p>`s (`AgentFeatures.tsx:67`, `InstallSteps.tsx:41`, `Intelligence.tsx:43`).
3. **Placeholder blocks where the reference shows product photography** - Reference: `image_original1.png` phone shows a leggings cut-out, two product photos with name + price, and the screen is filled to the orb; `home` tile reuses the same storefront inside the Shopify card; the video frame is a photo of a woman on her phone; each agent card carries a product-card mockup with the orb avatar. Build: `desktop-hero.png` phone = one grey gradient block + two empty grey tiles, and the screen is blank below 495 of 713 CSS px (36%; 47% at 390px in `mobile-hero.png`, where the chat bubble is also hidden); `desktop-shopify.png` phone = wireframe blocks; `desktop-demo.png` = radial gradient, no poster; agent cards = text bubbles only. Fix: generate with `higgsfield-imagery` - 3 product cut-outs (1:1), 1 hero product (3:4), 1 editorial 16:9 poster. `components/PhoneMockup.tsx:30,37-38` -> `<img width height>` with caption + price rows, `aspectRatio` `9/14` (or a second product row) so the orb overlaps content, and `hidden sm:block` on line 42 -> `block -right-2`; `Shopify.tsx:24-32` render `<PhoneMockup />` instead of the wireframe; `DemoVideo.tsx:23-26` add an `<img>` poster under `bg-black/40` and make the control a black pill "Watch demo" with a play glyph; `AgentFeatures.tsx` `Chat` gets `<Orb size={36} />` + a product thumbnail row.
4. **Grey surface bands the reference does not have, and the signature waveform used once** - Reference: the Home tile is one white canvas end to end; the gradient waveform recurs at the edges of shopify (right, top), install (top-right), demo (lower corners), intelligence (top-right) and faq (left). Build: manifest - `shopify`, `demo`, `faq` backgrounds `rgb(247,247,250)`; `svg.waveform` count = 1 (hero only). Fix: `Shopify.tsx:14`, `DemoVideo.tsx:11`, `Faq.tsx:22` `tone="surface"` -> `tone="ground"`; render `<Waveform className="opacity-40 -right-1/3 -top-24" />` (etc.) in those sections plus `InstallSteps` and `Intelligence`, passing `className="relative overflow-hidden"` to `Section`.
5. **FAQ anatomy is inverted** - Reference: 7 separate rows on white, each a surface-filled pill ~63px tall with ~18px gaps and a chevron-down at right; the open row turns white with a 1.5px gradient border and chevron-up. Build: `desktop-faq.png` - 8 rows inside one white 773x555 card with hairline dividers, on a grey band, "+"/"x" glyphs. Fix: `Faq.tsx:30` wrapper -> `space-y-3` (drop `divide-y border bg-white px-6`); each `<details>` -> `rounded-xl bg-[var(--color-surface)] px-5 py-3 open:[background:linear-gradient(#fff,#fff)_padding-box,linear-gradient(90deg,var(--color-accent),var(--color-accent2))_border-box] open:border open:border-transparent`; replace "+" with a chevron SVG `group-open:rotate-180`; drop one question to match 7.
6. **Install band anatomy** - Reference: a black "How it Works" tab sits on the band's top-left corner; inside, three plain columns - 24px line icon (download / bag / bolt) -> bold title -> body with a bold phrase - separated by thin "›" chevrons; no sub-cards. Build: `desktop-install.png` - three translucent sub-cards with "01/02/03" numerals, no icons, no chevrons, no tab. Fix: `InstallSteps.tsx:28-37` - remove `rounded-2xl bg-white/12 p-5 backdrop-blur-sm` from `<li>`, replace `{s.n}` with an inline 24px SVG icon per step (`stroke="currentColor" strokeWidth="1.8"`), add an `aria-hidden` "›" span between columns on `md:`, and insert `<span className="ml-6 inline-block rounded-t-xl bg-[#0A0A0A] px-4 py-2 text-[13px] font-semibold text-white">How it works</span>` directly above the band.
7. **Shopify right-column cards are text-only** - Reference: black card is ~45/55 - two product tiles + orb + "These 2 items are on sale!" bubble left, title + body right; gradient card is bubble + orb + title/body left and four stacked white payment tiles (Shop Pay, PayPal, G Pay, Apple Pay, ~130x44 each) right. Build: `desktop-shopify.png` - black card = title, body, quote box; gradient card = title, body, four inline 12px chips. Fix: `Shopify.tsx:42-65` - each `article` -> `grid gap-6 md:grid-cols-[1fr_1.2fr] items-center`; black card left cell = two mini `card-white` product tiles + `<Orb size={72} />` + the existing quote box as the bubble; gradient card right cell = `ul grid gap-2` of four `rounded-xl bg-white px-4 py-3 text-[14px] font-bold text-[var(--color-ink)]` tiles.
8. **CTA banner proportion and button role** - Reference: banner ~1426x483 at 1680 (2.95:1), H2 two lines ~60px, button is a dark ghost pill (dark fill, 1px light border, white text), waveform lines inside the card. Build: `desktop-cta.png` - 1200x345 (3.48:1), H2 36px, solid white pill. Fix: `Cta.tsx:14` `md:p-12` -> `md:p-16 md:min-h-[410px]`; add `<Waveform className="opacity-30 -bottom-40" />` inside the card; `globals.css` `.btn-inverse` -> `background:#1A1A1A; color:#fff; border:1px solid rgba(255,255,255,.3)`.
9. **Mobile tap targets under 44px** - Reference: no mobile capture; measured against the 44px floor. Build (390px, Playwright): FAQ `summary` hit area 292x26 (the `py-4` sits on `<details>`, not on the toggle), footer links 17px tall on 8px gaps, hamburger 40x40, wordmark 28px. Fix: `Faq.tsx:32-33` move `py-4` from `<details>` to `<summary>`; `Footer.tsx:28,34` links -> `inline-block py-2`; `Nav.tsx:64` `p-2` -> `p-2.5`.

## Polish
10. **Nav hairline hidden on load** - Reference: `image_original1.png` shows the 1px line under the nav at scroll 0. Build: `desktop-viewport.png` has none; `Nav.tsx:32` uses `border-transparent` until `scrollY > 24`. Fix: `border-black/8` in both branches.
11. **Footer details** - Reference: four social icon glyphs (facebook, twitter, instagram, tiktok), Product 5 / Legal 2, hairline above the copyright. Build: `desktop-footer.png` - text labels "X LinkedIn YouTube", Product 4 / Legal 3, no hairline. Fix: `Footer.tsx:18-24` -> 20px inline-SVG glyphs; `Footer.tsx:39` add `border-t border-black/8 pt-6`. Small footer type (13-14px) is *inherited* from the reference.
12. **An addition and verbatim template demo content** - Reference: nothing under the Shopify paragraph. Build: `Shopify.tsx:76` adds a "See how it installs" pill (LAYOUT.md's "paragraph + button" is wrong; the image outranks it). `PhoneMockup.tsx:17-44` copies the template's demo store word for word ("Quick Sport", "Betty Leggings", "Hi! Welcome to Quick Sport"). Fix: delete `Shopify.tsx:76`; rename the demo store and product in `PhoneMockup.tsx`.

## Excluded by design
colours (accent #7B3FE4 -> #16A34A, accent2 #E040A0 -> #0EA5E9, and every fill/stroke/text that carries them), fonts (Work Sans / Inter), logo mark and wordmark, copy wording, product name, photography subject - not reviewed.

## Not verified
- Reference mobile layout: `design-reference/` has no mobile capture, so mobile was checked only for overflow (none), reading order (matches desktop), and the 44px / 15px floors.
- Reference nav stickiness, hover/press states and the waveform drift: static images only. Build states were read from source and Playwright (accordion is exclusive via `details[name]`; mobile menu opens and closes; focus ring is a custom ink outline; reduced motion honoured).
- The demo video itself - the reference frame is a poster; the build has no media element yet.
